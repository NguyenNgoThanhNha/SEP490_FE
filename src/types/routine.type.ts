export type TRoutine = {
    skincareRoutineId: number,
    name: string,
    description: string,
    targetSkinTypes: string[],
    createdDate: string,
    updatedDate: string,
    totalSteps: number,
    totalPrice: number
}