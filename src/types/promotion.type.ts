export type TPromotion = {
    promotionId: number,
    promotionName: string,
    promotionDescription: string,
    discountPercent: number,
    startDate: string,
    endDate: string,
    status: string,
    createdDate: string,
    updatedDate: string,
    image: string
}